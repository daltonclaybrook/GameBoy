extension PPU {
    func generateDebugOAMImage() {
        (0..<40).forEach { index in
            let sprite = oam.getSprite(atIndex: index)
        }
    }

    // MARK: - Helpers

    private func getTiles(for sprite: SpriteAttributes) {
        switch sprite.
    }
}
